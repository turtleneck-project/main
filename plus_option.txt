/*var status ="good posture"
var count=0
async function predict() {
        // Prediction #1: run input through posenet
        // estimatePose can take in an image, video or canvas html element
        const { pose, posenetOutput } = await model.estimatePose(webcam.canvas);
        // Prediction 2: run input through teachable machine classification model
        const prediction = await model.predict(posenetOutput);
        if(prediction[0].probability.toFixed(2)==1.00){
            status="good posture"
        } else if (prediction[1].probability.toFixed(2)==1.00){
            status == "turtle neck"
        }
        for (let i = 0; i < maxPredictions; i++) {
            const classPrediction =
                prediction[i].className + ": " + prediction[i].probability.toFixed(2);
            labelContainer.childNodes[i].innerHTML = classPrediction;
        }

        //finally draw the poses
        drawPose(pose);
    }*/
    
위에 코드"prediction[0].probability.toFixed(2)==1.00"에서는 웹캠상에서 우리가 학습시킨 0번자세인
바른자세일 때 status를 good posture상태라하고 
만약 "prediction[1].probability.toFixed(2)==1.00" 우리가 학습한 상태가 1번 상태인 거북목 상태일경우
status를 turtle neck이라고 상태가 변화된다.

그런데 여기서 타이머 기능을 추가하여서 만약 거북목 1.00 상태가 10초동안 지속될경우 위에 정의해놓은
count 함수가 +1 되어서 자신이 거북목 상태를 얼마동안 몇번 하였는지 알려주는 기능들을 추가하고 싶다.
